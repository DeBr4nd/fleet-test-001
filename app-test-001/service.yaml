apiVersion: v1
kind: Service
metadata:
  name: app-test-001-service
  namespace: tests
spec:
  selector:
    app: app-test-001
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP